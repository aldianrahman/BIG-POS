<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BIGPOS" parent="Theme.Material3.Light.NoActionBar">
        <!-- Dark mode dimatikan: selalu pakai tema terang. -->
    </style>

    <style name="Theme.BIGPOS" parent="Base.Theme.BIGPOS" />
</resources>